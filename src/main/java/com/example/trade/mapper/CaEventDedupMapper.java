package com.example.trade.mapper;

import com.example.trade.domain.CaEventDedup;
import com.example.trade.domain.TradeEventDedup;

public interface CaEventDedupMapper {
    int insert(CaEventDedup dedup);
}
